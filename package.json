{
  "scripts": {
    "start": "concurrently --kill-others \"npx serve\" \"npx json-server db.json -p 3001\""

  },
  "devDependencies": {
    "concurrently": "^7.2.1",
    "json-server": "^0.17.0"
  }
}
